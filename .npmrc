# Required for React 19 compatibility with packages that declare older peer deps
# (e.g. lucide-react-native, @expo-google-fonts packages)
# The "overrides" block in package.json forces a single React copy for all.
legacy-peer-deps=true
